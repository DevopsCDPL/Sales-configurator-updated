MODEL ROUTING & TOKEN ECONOMY (Vikraman policy, 2026-06-12).

Vikraman asked to be CORRECTED whenever a task could be done by a lower model in a fresh chat, and to always keep token optimization in mind.

ROUTING POLICY (explicit): Fable 5 = orchestrator + high-end only — assigns tasks (writes handoff briefs) and does complex engineering + complex UI itself. Anything pausable/pushable goes down. Available in the same project: Sonnet 4.6 and Opus 4.8. Fable DECIDES which model gets what.
- Sonnet → routine (styling, color/spacing, copy, repeat-pattern screens, test runs, deploy walkthroughs, doc formatting).
- Opus → mid-complexity (single-feature endpoints following existing patterns, moderate refactors, multi-file features with judgment).
- Fable → architecture, engines, money/NEC math, cross-stack debugging, TPS data analysis, task assignment + verification.

HOW TO APPLY:
- At the start of a request, if routine, say so and hand off a ready-to-paste brief (repo path, files, exact change, verify command).
- Reserve Fable for architecture, multi-file engine work, pricing/NEC logic, cross-stack debugging, data analysis, trade-off decisions.
- Token discipline: batch UI nits instead of one-message-per-tweak; avoid re-reading large files; prefer surgical greps; commit early so context loss is cheap.
- VERIFICATION RULE (from G23 incident): never accept an agent's commit without re-checking tsc EXIT:0 and file tails yourself — agents have committed truncated files and rationalized broken builds.

NOTE: if Fable 5 is retired, this policy still applies with whatever models are available — the orchestrator role can be any top model; route routine work down.
